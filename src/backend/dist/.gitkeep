Placeholder so `go build` has something to embed before `deno task build` has run.
The real contents are generated by `deno task build` into this directory and are
gitignored (see the repo's `.gitignore`, which excludes any `dist/`).
